<?php return [

	/**
	 * Public Prefixes
	 *
	 * The keys in this array correspond to disks configured in `filesystems.php`  Values specified
	 * will be appended with the files path when requesting public URIs.
	 */
	'public_prefixes' => [

		// 'images' => '/images'

	]

];
